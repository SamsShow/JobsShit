services:
  - type: worker
    name: telegram-job-tracker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python job_tracker_bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0


